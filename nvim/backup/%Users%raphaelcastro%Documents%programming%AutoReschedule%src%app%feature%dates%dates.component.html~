<mat-form-field appearance="fill">
	<mat-label>Click Here!</mat-label>
	<input
		[matDatepickerFilter]="removeSoldOut"
		[min]="min"
		(focus)="picker.open()"
		[(ngModel)]="_date"
		matInput
		[matDatepicker]="picker"
	/>
	<mat-datepicker-toggle matSuffix [for]="picker"></mat-datepicker-toggle>
	<mat-datepicker #picker></mat-datepicker>
</mat-form-field>
<p *ngIf="_date">
	Due to limited availability please confirm the dates you have selected are
	perfect for your schedule.
</p>
<button
	*ngIf="_date"
	(click)="sendDates()"
	mat-raised-button
	mat-button
	color="primary"
>
	Confirm
</button>
