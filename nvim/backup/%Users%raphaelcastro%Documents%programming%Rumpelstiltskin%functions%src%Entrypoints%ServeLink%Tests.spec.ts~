import {createQuickbaseQuery} from "../../Shared/Quickbase";

/* <-- Import End --> */

const from = "bmhvhc72c"
const id = 61027
const select = [533]

/* <-- Variable Definition End --> */

describe("Serve Link", () => {
  beforeEach(() => {}); 
  it("Should send the proper query to the quickbase read method", async () => {
    const snap = createQuickbaseQuery({from, id, select})
    console.log(snap)
    expect(snap).toMatchSnapshot()
  });
});
